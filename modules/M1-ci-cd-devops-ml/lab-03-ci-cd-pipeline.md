# Лабораторная работа 03. Создание CI/CD pipeline для ML-проекта

## Цель работы

Автоматизировать проверки качества, тестирование, сборку и поставку ML-проекта с использованием воспроизводимого CI/CD pipeline.

## Задачи работы

1. Добавить автоматические проверки качества кода и данных.
2. Настроить CI на запуск тестов и проверку минимального порога качества модели.
3. Собирать Docker-образ только после успешной валидации.
4. Публиковать артефакт или имитировать deploy при контролируемых условиях.
5. Продемонстрировать как успешный, так и падающий сценарий pipeline.

## Необходимое программное обеспечение

- Linux или Windows 10/11 с WSL2
- Python 3.10+
- Git
- Docker Engine или Docker Desktop
- GitHub Actions или Jenkins
- редактор кода или IDE

## Краткие теоретические сведения

CI отвечает за автоматическую проверку каждого изменения, а CD подготавливает или выполняет поставку. В ML-системах pipeline должен валидировать не только код приложения, но и предпосылки по данным, воспроизводимость обучения и пороги качества модели.

## Задание

- Организовать репозиторий и ветвление в соответствии с выбранным сценарием работы.
- Добавить минимум два unit-теста и один тест качества данных.
- Добавить линтер, проверку форматирования или этап статического анализа.
- Реализовать workflow или Jenkins pipeline согласно назначенному варианту.
- Настроить кэширование Python-зависимостей.
- Выполнять обучение на уменьшенной выборке в CI и завершать pipeline ошибкой, если порог метрики не достигнут.
- Собирать Docker-образ после успешных проверок.
- Архивировать отчеты тестов и метрики как CI-артефакты.
- Добавить условный этап публикации образа или имитации deploy только для основной ветки или тега.
- Проверить pipeline на успешном и намеренно ошибочном commit.

## Индивидуальные варианты

| Вариант | Система | Проверки | Порог | Этап доставки |
|---|---|---|---|---|
| 1 | GitHub Actions | `pytest` + `ruff` | `accuracy >= 0.90` | build без публикации |
| 2 | GitHub Actions | `pytest` + `flake8` | `F1 >= 0.85` | публикация по тегу |
| 3 | GitHub Actions | `pytest` + `mypy` | `RMSE <= заданного порога` | build + artifact |
| 4 | GitHub Actions | `pytest` + `black --check` | `ROC-AUC >= 0.90` | публикация `main` |
| 5 | Jenkins Declarative Pipeline | `pytest` + `pylint` | `accuracy >= 0.80` | локальный registry |
| 6 | Jenkins Declarative Pipeline | `pytest` + `ruff` | `MAE <= заданного порога` | архивирование модели |
| 7 | GitHub Actions | `unittest` + `flake8` | проверка числа пропусков | build matrix Python 3.10/3.11 |
| 8 | GitHub Actions | `pytest` + `bandit` | `silhouette >= 0.40` | security report |
| 9 | Jenkins Pipeline | `pytest` + `mypy` | `F1 >= 0.75` | ручное подтверждение deploy |
| 10 | GitHub Actions | `pytest` + `ruff` | `latency smoke-test < 1 c` | публикация по release |

## Требования к отчету

- титульный лист с указанием дисциплины, номера и темы лабораторной работы, Ф.И.О. студента, группы и варианта;
- цель работы и индивидуальное задание;
- этапы pipeline и триггеры;
- набор проверок и обоснование их выбора;
- хранение артефактов и метрик;
- подтверждение одного успешного и одного падающего прогона;
- описание возникших ошибок и способов их устранения;
- вывод о готовности решения к командной разработке.

## Контрольные вопросы

1. Что может выступать триггером CI pipeline?
2. Чем этап CI отличается от CD?
3. Какие проверки особенно важны для ML-проектов?
4. Почему проверки качества модели не заменяют unit-тесты?
5. Для чего используется кэш зависимостей?
6. Что такое артефакт pipeline?
7. Как предотвратить публикацию образа при провале теста?
8. Как безопасно хранить токены реестра контейнеров?
9. Что такое матричная сборка?
10. Почему pipeline должен проверяться на намеренно ошибочном изменении?

## Критерии оценивания

| Критерий | Наблюдаемые показатели | Дифференциация баллов | Баллы |
|---|---|---|---|
| Ответы на контрольные вопросы | Правильные, развернутые ответы на 10 вопросов | 0 — нет ответа; 1 — ответ неполный; 2 — полный правильный ответ | 2 |
| Демонстрация работы | Pipeline успешно выполняется на успешном и ошибочном коммите | 0 — не запускается; 1 — работает корректно | 1 |
| Оформление отчета | Все разделы заполнены, есть скриншоты и выводы | 0 — не оформлен; 1 — оформлен по требованиям | 1 |
| Качество кода | Pipeline оптимален, учтены кэширование, артефакты, безопасность секретов | 0 — не работает; 1 — работает, но есть недочеты; 2 — код оптимален | 2 |
| Индивидуальное задание | Настроен workflow, добавлены тесты, сборка Docker-образа в pipeline, условный этап доставки | 0 — не выполнено; 1 — выполнено частично; 2 — выполнены базовые этапы, но есть ошибки; 3 — все основные этапы выполнены, но нет условного этапа доставки; 4 — полностью выполнено | 4 |
| Итого |  |  | 10 |

## Минимальный порог

5 баллов.

## Типовые ошибки

1. Нет тестов кода или данных.
2. Отсутствует кэширование зависимостей.
3. Секреты (токены) захардкожены в коде.
4. Нет условного этапа доставки.
5. Pipeline не проверяет порог качества модели.
6. Нет артефактов (отчетов, метрик).
7. Pipeline не тестировался на ошибочном коммите.

## Правила защиты

1. Демонстрация работы — не более 10 минут.
2. Студент показывает успешный запуск pipeline на корректном коммите, запуск на ошибочном коммите (должен упасть) и артефакты сборки.
3. Студент отвечает минимум на 5 контрольных вопросов из 10 по выбору преподавателя.

## Связанные компетенции и индикаторы

- BD-5.3
- LC-5.1

## Публикационные и организационные правила (не критерии оценивания)

- [Эталонный workflow GitHub Actions](../../.github/workflows/reference-project-ci.yml): пример проверки baseline из корня репозитория; используйте его как ориентир для собственной конфигурации CI.
- Ожидаемое время: 4 академических часа. До защиты представьте конфигурацию, логи успешного и намеренно падающего запуска, команды воспроизведения и отчет.
