# Лабораторная работа 06. Отслеживание изменений в моделях и данных с DVC

## Цель работы

Научиться версионировать данные, модели и этапы ML-процесса с помощью DVC и воспроизводить прошлые состояния проекта вместе с ревизиями Git.

## Задачи работы

1. Инициализировать DVC в ML-проекте на базе Git.
2. Настроить удаленное хранилище вне репозитория.
3. Добавить датасеты и результаты модели под контроль DVC.
4. Описать workflow в `dvc.yaml`.
5. Сравнить метрики между ревизиями и восстановить предыдущее состояние.

## Необходимое программное обеспечение

- Linux или Windows 10/11 с WSL2
- Python 3.10+
- Git
- DVC
- редактор кода или IDE

## Краткие теоретические сведения

Git хорошо подходит для исходного кода и легковесных метаданных, а DVC расширяет контроль версий на большие датасеты, обученные модели и воспроизводимые pipeline. Связка Git-тегов и DVC-метаданных позволяет восстановить как состояние кода, так и состояние данных для конкретного эксперимента.

## Задание

- Инициализировать Git и DVC.
- Настроить локальный `remote`-каталог вне репозитория.
- Добавить исходные данные под DVC-контроль и выполнить `push`.
- Описать в `dvc.yaml` как минимум этапы `prepare`, `train` и `evaluate`.
- Объявить параметры, зависимости, выходы и файл метрик.
- Выполнить первую версию pipeline и создать Git commit с метафайлами DVC.
- Изменить данные по варианту, пересчитать pipeline и сравнить метрики между версиями.
- Для варианта с дисбалансом сопоставить `accuracy` с устойчивой к дисбалансу метрикой и объяснить изменение результата.
- Создать Git-тег для каждой версии.
- Восстановить предыдущую отмеченную тегом ревизию через `dvc pull` и подтвердить воспроизводимость.
- Сформировать граф pipeline и включить его в отчет.

## Индивидуальные варианты

| Вариант | Изменение данных | Метрики | Цель анализа |
|---|---|---|---|
| 1 | Добавить 10% корректных объектов | `accuracy` / `F1` | ожидается устойчивость или рост |
| 2 | Удалить 10% объектов одного класса | `macro-F1` | проанализировать дисбаланс |
| 3 | Добавить 5% пропусков | `MAE` / `accuracy` | изменить preprocessing |
| 4 | Добавить шум к числовым признакам | `ROC-AUC` | оценить деградацию |
| 5 | Изменить правило `train/test split` | `accuracy` | сравнить вариативность |
| 6 | Добавить новый признак | `MAE` / `R²` | оценить полезность |
| 7 | Удалить один признак | `F1` | оценить влияние |
| 8 | Изменить словарь текстов | `macro-F1` | сравнить размер словаря |
| 9 | Изменить разрешение изображений | `accuracy` / `время` | сравнить ресурсы |
| 10 | Изменить число кластеров или состав данных | `silhouette` | сравнить структуру |

## Требования к отчету

- титульный лист с указанием дисциплины, номера и темы лабораторной работы, Ф.И.О. студента, группы и варианта;
- цель работы и индивидуальное задание;
- настройка DVC и удаленного хранилища;
- граф pipeline и отслеживаемые артефакты;
- сравнение минимум двух ревизий;
- обоснование изменения качества по релевантным метрикам;
- подтверждение восстановления предыдущей версии;
- описание возникших ошибок и способов их устранения;
- выводы о трассируемости данных и моделей.

## Контрольные вопросы

1. Почему большие датасеты не рекомендуется хранить напрямую в Git?
2. Что хранится в `.dvc`-файле?
3. Для чего нужен DVC cache?
4. Чем `dvc.yaml` отличается от `dvc.lock`?
5. Что обозначают `deps` и `outs`?
6. Как DVC определяет необходимость повторного запуска этапа?
7. Для чего используются `dvc push` и `dvc pull`?
8. Как связаны Git-тег и версия данных DVC?
9. Как сравнить метрики нескольких ревизий?
10. Какие риски возникают при недоступности удаленного DVC-хранилища?

## Критерии оценивания

| Критерий | Наблюдаемые показатели | Дифференциация баллов | Баллы |
|---|---|---|---|
| Ответы на контрольные вопросы | Правильные, развернутые ответы на 10 вопросов | 0 — нет ответа; 1 — ответ неполный; 2 — полный правильный ответ | 2 |
| Демонстрация работы | DVC-пайплайн воспроизводится, данные и модели версионируются | 0 — не запускается; 1 — работает корректно | 1 |
| Оформление отчета | Все разделы заполнены, есть скриншоты и выводы | 0 — не оформлен; 1 — оформлен по требованиям | 1 |
| Качество кода | Учтены зависимости, параметры, метрики, этапы описаны в dvc.yaml | 0 — не работает; 1 — работает, но есть недочеты; 2 — код соответствует лучшим практикам | 2 |
| Индивидуальное задание | Инициализирован DVC, настроено удаленное хранилище, описаны этапы, выполнено сравнение релевантных метрик двух версий, продемонстрирована воспроизводимость | 0 — не выполнено; 1 — выполнено частично; 2 — выполнены базовые этапы, но есть ошибки; 3 — все основные этапы выполнены, но нет сравнения метрик, remote или воспроизводимости; 4 — полностью выполнено | 4 |
| Итого |  |  | 10 |

## Минимальный порог

5 баллов.

## Типовые ошибки

1. Данные добавлены в Git, а не в DVC.
2. Не настроено удаленное хранилище.
3. Нет графа pipeline.
4. Не сравниваются метрики двух версий.
5. Нет Git-тегов.
6. Не воспроизведена модель из предыдущей версии.
7. Нет файла dvc.lock.
8. Изменение данных не связано с интерпретацией изменения метрик.

## Правила защиты

1. Демонстрация работы — не более 10 минут.
2. Студент показывает граф pipeline (`dvc dag`), сравнение метрик двух версий (`dvc metrics diff`) и восстановление модели после переключения на предыдущий тег.
3. Студент отвечает минимум на 5 контрольных вопросов из 10 по выбору преподавателя.

## Связанные компетенции и индикаторы

- BD-5.3
- ML-2.2
- ML-2.3
- LC-5.2

## Публикационные и организационные правила (не критерии оценивания)

- [Эталонный baseline: DVC](../../resources/reference-mlops-project/dvc.yaml)
- Baseline содержит локальный воспроизводимый pipeline и `dvc.lock`, но не содержит remote и учебных Git-тегов. Настройте собственный remote, создайте две ревизии и теги для сдачи работы.
- Ожидаемое время: 4 академических часа. До защиты представьте `dvc.yaml`, lock-файл, две ревизии, сравнение релевантных метрик и восстановление из remote.
