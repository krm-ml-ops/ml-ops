# Лабораторная работа 04. Простая реализация ML pipeline

## Цель работы

Реализовать воспроизводимый ML pipeline от загрузки данных до сохранения модели с явной валидацией, конфигурацией и метриками.

## Задачи работы

1. Разделить workflow на понятные этапы обработки.
2. Отделить конфигурацию от кода.
3. Валидировать входные данные перед обучением.
4. Исключить утечку данных на этапе preprocessing.
5. Сохранять модель, конфигурацию и метрики оценки.

## Необходимое программное обеспечение

- Linux или Windows 10/11 с WSL2
- Python 3.10+
- Git
- Docker Engine или Docker Desktop
- scikit-learn
- pandas
- PyYAML или инструменты конфигурации на основе JSON

## Краткие теоретические сведения

ML pipeline формализует последовательность шагов от сырых данных до оцененных модельных артефактов. Явные границы этапов упрощают отладку, воспроизводимость и последующую автоматизацию. Управление выполнением через конфигурацию является основой надежного управления экспериментами и жизненным циклом. В рамках работы преподаватель может намеренно внести в открытый набор данных пропуски, выбросы, дублирующиеся записи и некорректные значения; до обучения необходимо проанализировать качество данных и подготовить очищенный набор.

## Задание

- Выполнить анализ качества исходного набора данных: выявить пропуски, выбросы, дублирующиеся записи и некорректные значения, подготовить их описание.
- Реализовать отдельный этап `prepare` для очистки данных, обработки пропусков, устранения или обработки выбросов, приведения типов и сохранения подготовленного набора.
- Обеспечить возможность повторного запуска этапа `prepare` без изменения остальных компонентов pipeline при неизменности исходных данных.
- Реализовать этапы `load`, `validate`, `split`, `preprocess`, `train`, `evaluate` и `save`.
- Хранить параметры в YAML или JSON.
- Сформировать журнал выполнения с длительностью этапов.
- Проверять схему данных, пропуски и допустимые диапазоны.
- Использовать `sklearn.pipeline.Pipeline` или эквивалентную функциональную схему.
- Сохранять обученную модель, метрики, конфигурацию и сведения о версии данных.
- Реализовать повторный запуск одной командой.
- Добавить минимум два автоматических теста: на валидацию данных и на форму результата pipeline.

## Индивидуальные варианты

| Вариант | Данные | Pipeline | Метрики |
|---|---|---|---|
| 1 | Iris | `StandardScaler + LogisticRegression` | `accuracy`, `macro-F1` |
| 2 | Wine | `StandardScaler + SVC` | `accuracy`, `macro-F1` |
| 3 | Breast Cancer | `StandardScaler + RandomForest` | `ROC-AUC`, `F1` |
| 4 | Diabetes | `StandardScaler + Ridge` | `MAE`, `RMSE` |
| 5 | Digits | `MinMaxScaler + LogisticRegression` | `accuracy`, `confusion matrix` |
| 6 | Синтетическая классификация | `SimpleImputer + RandomForest` | `accuracy`, `ROC-AUC` |
| 7 | Синтетическая регрессия | `SimpleImputer + RandomForestRegressor` | `MAE`, `R²` |
| 8 | Смешанные признаки из CSV | `ColumnTransformer + LogisticRegression` | `F1`, `ROC-AUC` |
| 9 | Текстовая классификация | `TfidfVectorizer + LinearSVC` | `accuracy`, `macro-F1` |
| 10 | Кластеризация | `StandardScaler + KMeans` | `silhouette`, `inertia` |

## Требования к отчету

- титульный лист с указанием дисциплины, номера и темы лабораторной работы, Ф.И.О. студента, группы и варианта;
- цель работы и индивидуальное задание;
- архитектура pipeline и схема конфигурации;
- стратегия валидации;
- журналы выполнения и длительности этапов;
- сохраненные артефакты и замечания по воспроизводимости;
- подтверждение тестов и измеренные метрики;
- описание возникших ошибок и способов их устранения;
- выводы о модульности pipeline.

## Контрольные вопросы

1. Какие этапы обычно входят в ML pipeline?
2. Что такое утечка данных?
3. Почему preprocessing должен обучаться только на train-выборке?
4. Какие преимущества дает конфигурационный файл?
5. Чем валидация данных отличается от оценки модели?
6. Для чего сохранять версию данных вместе с моделью?
7. Как обеспечить идемпотентность этапа pipeline?
8. Почему важно логировать длительность этапов?
9. Что должно входить в артефакты обучения?
10. Какие части pipeline целесообразно покрыть автоматическими тестами?

## Критерии оценивания

- Ответы на контрольные вопросы — 15 баллов
- Демонстрация работоспособности решения — 15 баллов
- Оформление отчета — 10 баллов
- Качество программной реализации — 20 баллов
- Выполнение индивидуального задания — 40 баллов

## Связанные компетенции и индикаторы

- ML-2.2
- LC-3.1

## Оценивание и ресурс

- [Рубрика ЛР 04](../lab-rubrics.md)
- [Эталонный baseline: pipeline](../../resources/reference-mlops-project/README.md)
- Ожидаемое время: 4 академических часа. До защиты представьте конфигурацию, тесты валидации, модель, метрики и команду повторного запуска.
