# Лабораторная работа 08. Мониторинг модели: Prometheus и Grafana

## Цель работы

Инструментировать ML-сервис, собирать технические и модельные метрики и анализировать поведение системы с помощью Prometheus и Grafana.

## Задачи работы

1. Экспортировать метрики сервиса из FastAPI-приложения.
2. Развернуть Prometheus и Grafana через Docker Compose.
3. Построить dashboard по сервисным и модельным показателям.
4. Сгенерировать нормальную нагрузку, всплески и невалидные запросы.
5. Настроить и проверить alert-правила.

## Необходимое программное обеспечение

- Linux или Windows 10/11 с WSL2
- Python 3.10+
- Git
- Docker Engine или Docker Desktop
- Docker Compose
- Prometheus
- Grafana
- `prometheus-client`

## Краткие теоретические сведения

Мониторинг в MLOps охватывает инфраструктуру, поведение сервиса и поведение модели. Так как истинные метки часто поступают с задержкой, эксплуатационный контроль опирается и на прокси-индикаторы: распределение предсказаний, drift входных данных, задержки и частоту ошибок.

## Задание

- Расширить сервис лабораторной работы 07 endpoint `/metrics`.
- Добавить счетчик запросов по endpoint и статусу, histogram задержки, gauge активных запросов и счетчик ошибок.
- Добавить не менее двух модельных метрик, определенных вариантом.
- Настроить Docker Compose для сервиса, Prometheus и Grafana.
- Настроить `scrape`-конфигурацию Prometheus.
- Создать Grafana dashboard минимум из шести панелей.
- Сгенерировать нормальную нагрузку, всплеск нагрузки и серию невалидных запросов.
- Описать один технический и один модельный alert, намеренно вызвать оба и сохранить подтверждение срабатывания.
- Сформулировать вывод о состоянии сервиса и возможной деградации модели.

## Индивидуальные варианты

| Вариант | ML-метрика | Модельный alert | Технический alert |
|---|---|---|---|
| 1 | Доля классов прогнозов | рост доли одного класса > 80% | `p95 latency > 0.5 c` |
| 2 | Средняя confidence | `confidence < 0.6` | `error rate > 5%` |
| 3 | Число значений вне train-диапазона | `out-of-range > 10%` | `service down` |
| 4 | Сдвиг среднего ключевого признака | отклонение > `2σ` | `p99 latency > 1 c` |
| 5 | Доля пропусков или нулевых значений | `missing > 5%` | `requests/sec > лимита` |
| 6 | Распределение batch size | `batch > 100` | `memory proxy gauge > порога` |
| 7 | Средний прогноз регрессии | изменение > 30% `baseline` | `error rate > 3%` |
| 8 | Распределение кластеров | пустой или доминирующий кластер | `p95 > 0.7 c` |
| 9 | Длина текста и доля пустых строк | `empty > 2%` | `HTTP 422 > порога` |
| 10 | Размер изображения или вектора | `invalid shape > 1%` | `throughput < порога` |

## Требования к отчету

- титульный лист с указанием дисциплины, номера и темы лабораторной работы, Ф.И.О. студента, группы и варианта;
- цель работы и индивидуальное задание;
- архитектура мониторинга и `scrape`-конфигурация;
- обзор dashboard;
- alert-правила и подтверждение их срабатывания;
- анализ состояния сервиса и возможной деградации модели;
- описание возникших ошибок и способов их устранения;
- итоговые рекомендации.

## Контрольные вопросы

1. Какие группы метрик важны для мониторинга ML-систем?
2. Чем `Counter` отличается от `Gauge`?
3. Когда предпочтительно использовать `Histogram`?
4. Как Prometheus получает метрики приложения?
5. Что такое `label` и почему высокая кардинальность опасна?
6. Как вычислить долю ошибок по временным рядам?
7. Что показывает `p95` задержки?
8. Чем data drift отличается от concept drift?
9. Почему отсутствие `ground truth` усложняет мониторинг качества модели?
10. Какие элементы должен содержать полезный Grafana dashboard?

## Критерии оценивания

| Критерий | Наблюдаемые показатели | Дифференциация баллов | Баллы |
|---|---|---|---|
| Ответы на контрольные вопросы | Правильные, развернутые ответы на 10 вопросов | 0 — нет ответа; 1 — ответ неполный; 2 — полный правильный ответ | 2 |
| Демонстрация работы | Метрики собираются и отображаются в Grafana | 0 — не запускается; 1 — работает корректно | 1 |
| Оформление отчета | Все разделы заполнены, есть скриншоты и выводы | 0 — не оформлен; 1 — оформлен по требованиям | 1 |
| Качество кода | Учтены типы метрик, настройка алертов, код модульный | 0 — не работает; 1 — работает, но есть недочеты; 2 — код соответствует лучшим практикам | 2 |
| Индивидуальное задание | Добавлены технические и ML-метрики, развернуты Prometheus и Grafana, создан dashboard, настроены alert rules | 0 — не выполнено; 1 — выполнено частично; 2 — выполнены базовые этапы, но есть ошибки; 3 — все основные этапы выполнены, но нет алертов или смоделированного срабатывания; 4 — полностью выполнено | 4 |
| Итого |  |  | 10 |

## Минимальный порог

5 баллов.

## Типовые ошибки

1. Не добавлены ML-метрики.
2. Нет дашборда в Grafana.
3. Нет алертов.
4. Не смоделировано срабатывание алерта.
5. Нет нагрузки для тестирования метрик.
6. Нет вывода о деградации модели.
7. Не настроена scrape-конфигурация Prometheus.

## Правила защиты

1. Демонстрация работы — не более 10 минут.
2. Студент показывает дашборд Grafana, генерирует нагрузку и показывает срабатывание алерта.
3. Студент отвечает минимум на 5 контрольных вопросов из 10 по выбору преподавателя.

## Связанные компетенции и индикаторы

- BD-5.1
- BD-5.3
- LC-5.1

## Публикационные и организационные правила (не критерии оценивания)

- [Эталонный baseline: мониторинг](../../resources/reference-mlops-project/monitoring/)
- Ожидаемое время: 4 академических часа. До защиты представьте scrape-конфигурацию, dashboard, два проверенных alert и логи нагрузки.
