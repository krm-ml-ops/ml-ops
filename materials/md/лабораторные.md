**МИНИСТЕРСТВО ЦИФРОВОГО РАЗВИТИЯ, СВЯЗИ И МАССОВЫХ  
КОММУНИКАЦИЙ РОССИЙСКОЙ ФЕДЕРАЦИИ**

Ордена Трудового Красного Знамени федеральное государственное бюджетное  
образовательное учреждение высшего образования  
«Московский технический университет связи и информатики» (МТУСИ)

**МЕТОДИЧЕСКИЕ УКАЗАНИЯ  
К ВЫПОЛНЕНИЮ ЛАБОРАТОРНЫХ РАБОТ**

**по дисциплине  
«ОСНОВЫ MLOps»**

Направление подготовки: 09.03.01 «Информатика и вычислительная техника»

Направленность (профиль): «ТОП-ИИ: Инженерия систем искусственного интеллекта»

Квалификация выпускника: бакалавр

Форма обучения: очная

<br/><br/>

Москва, 2026 г.

# Общие положения

Методические указания предназначены для организации лабораторного практикума по дисциплине «Основы MLOps». Содержание работ соответствует рабочей программе дисциплины и охватывает подготовку воспроизводимого окружения, контейнеризацию, автоматизацию CI/CD, построение ML-пайплайнов, управление экспериментами и версиями данных, развёртывание моделей в виде API-сервисов, а также мониторинг ML-систем.

Каждая лабораторная работа выполняется по индивидуальному варианту. Номер варианта, как правило, определяется преподавателем либо вычисляется по номеру студента в журнале группы. Допускается корректировка исходных наборов данных и технических ограничений преподавателем при сохранении целей и планируемых результатов работы.

## Общие требования к организации работы

- Все исходные тексты, конфигурации и инструкции запуска должны храниться в Git-репозитории.
- Проект должен запускаться по документированной последовательности команд на чистом рабочем месте.
- Секреты, токены и пароли запрещается включать в репозиторий и отчёт.
- При использовании внешнего набора данных в отчёте необходимо указать источник и лицензионные ограничения.
- Программный код должен содержать обработку ошибок, минимальные комментарии и понятные имена сущностей.
- Результат каждой работы защищается демонстрацией работающего решения и ответами на контрольные вопросы.

# Лабораторная работа № 1. Docker и настройка воспроизводимого окружения

## Цель работы

Освоить базовые принципы контейнеризации и подготовить воспроизводимое программное окружение для ML-проекта.

## Задачи работы

- установить и проверить Docker и Docker Compose;
- создать структуру учебного ML-проекта;
- зафиксировать зависимости проекта;
- подготовить базовый Dockerfile;
- собрать образ и проверить запуск Python-кода внутри контейнера;
- сопоставить контейнеризацию с использованием виртуального окружения Python.

## Необходимое программное обеспечение

- ОС Windows 10/11 с WSL2 или Linux;
- Python 3.10 или новее;
- Git;
- Docker Engine / Docker Desktop;
- редактор кода или IDE.

## Краткие теоретические сведения

Воспроизводимое окружение фиксирует версии интерпретатора, системных библиотек и Python-зависимостей. Docker-образ является неизменяемым шаблоном, а контейнер — запущенным экземпляром этого шаблона. Для ML-проектов особенно важно контролировать версии библиотек обработки данных и машинного обучения, поскольку изменение зависимостей может повлиять на результаты обучения и инференса.

## Общее задание

- Создать Git-репозиторий со структурой \`src/\`, \`data/\`, \`tests/\`, \`requirements.txt\`, \`Dockerfile\`, \`README.md\`.
- Реализовать скрипт \`src/check_environment.py\`, который выводит версии Python и ключевых библиотек, загружает небольшой набор данных и выполняет одну операцию предобработки.
- Зафиксировать версии зависимостей в \`requirements.txt\` или \`pyproject.toml\`.
- Подготовить Dockerfile на основе назначенного базового образа.
- Собрать образ с тегом \`mlops-lab1:<вариант>\`.
- Запустить контейнер, смонтировав каталог \`data/\` в режиме чтения.
- Сравнить вывод скрипта на хосте и в контейнере.

## Индивидуальные варианты

| **Вариант** | **Задача / данные**                          | **Базовый образ**    | **Обязательные зависимости**     |
| ----------- | -------------------------------------------- | -------------------- | -------------------------------- |
| 1           | Классификация Iris                           | python:3.11-slim     | pandas, scikit-learn, joblib     |
| 2           | Классификация Wine                           | python:3.10-slim     | numpy, pandas, scikit-learn      |
| 3           | Регрессия Diabetes                           | python:3.11-bookworm | pandas, scikit-learn, matplotlib |
| 4           | Классификация Breast Cancer                  | python:3.12-slim     | numpy, scikit-learn, pydantic    |
| 5           | Классификация Digits                         | python:3.11-slim     | numpy, scikit-learn, pillow      |
| 6           | Синтетическая бинарная классификация         | python:3.10-bookworm | numpy, pandas, scikit-learn      |
| 7           | Синтетическая регрессия                      | python:3.11-slim     | numpy, pandas, scikit-learn      |
| 8           | Кластеризация синтетических данных           | python:3.12-slim     | numpy, scikit-learn, matplotlib  |
| 9           | Анализ текстовых сообщений из локального CSV | python:3.11-slim     | pandas, scikit-learn, nltk       |
| 10          | Анализ изображений из локального каталога    | python:3.10-slim     | numpy, pillow, scikit-learn      |

## Требования к содержанию отчёта

- титульный лист с указанием дисциплины, номера и темы лабораторной работы, Ф.И.О. студента, группы и варианта;
- цель работы и индивидуальное задание;
- краткое описание архитектуры решения и используемых инструментов;
- листинги ключевых конфигурационных файлов и программного кода;
- снимки экрана или текстовые журналы, подтверждающие успешное выполнение основных этапов;
- результаты проверки, тестирования или измерения метрик;
- описание возникших ошибок и способов их устранения;
- выводы по результатам выполнения работы.

## Контрольные вопросы

1\. Что входит в понятие воспроизводимого окружения?

2\. Чем Docker-образ отличается от контейнера?

3\. Для чего в Dockerfile используется инструкция WORKDIR?

4\. Почему версии зависимостей желательно фиксировать?

5\. Как работает монтирование каталога в контейнер?

6\. В чём различие между COPY и bind mount?

7\. Какие преимущества и ограничения имеют slim-образы?

8\. Почему не следует запускать приложение в контейнере от пользователя root?

9\. Что такое слой Docker-образа?

10\. Как проверить, что контейнеризированное окружение действительно воспроизводимо?

# Лабораторная работа № 2. Создание Docker-образа и работа с контейнерами

## Цель работы

Научиться проектировать оптимизированный Docker-образ ML-приложения и управлять жизненным циклом контейнеров.

## Задачи работы

- реализовать учебное приложение обучения или инференса;
- применить эффективное кэширование слоёв Docker;
- настроить переменные окружения и параметры запуска;
- добавить healthcheck;
- использовать том для сохранения артефактов;
- оценить размер и содержимое образа.

## Необходимое программное обеспечение

- ОС Windows 10/11 с WSL2 или Linux;
- Python 3.10 или новее;
- Git;
- Docker Engine / Docker Desktop;
- редактор кода или IDE.

## Краткие теоретические сведения

Качество Docker-образа определяется не только работоспособностью, но и размером, безопасностью, скоростью сборки и предсказуемостью запуска. Разделение копирования файла зависимостей и исходного кода позволяет использовать кэш слоёв. Для сохранения моделей и журналов вне жизненного цикла контейнера применяются volumes или bind mounts.

## Общее задание

- Создать приложение \`train.py\` или \`predict.py\` в соответствии с вариантом.
- Подготовить \`.dockerignore\`.
- Разработать Dockerfile с отдельным этапом установки зависимостей; для указанных вариантов применить multi-stage build.
- Передать настройки через переменные окружения и аргументы командной строки.
- Создать непривилегированного пользователя внутри образа.
- Добавить HEALTHCHECK или отдельную команду самопроверки.
- Сохранить модель или отчёт в смонтированный каталог \`/artifacts\`.
- Проверить команды \`docker inspect\`, \`docker logs\`, \`docker exec\`, \`docker stats\`, остановку и удаление контейнера.
- Зафиксировать размер образа и время повторной сборки после изменения одного файла исходного кода.

## Индивидуальные варианты

| **Вариант** | **Приложение**                      | **Сборка**    | **Параметр окружения**     | **Выходной артефакт** |
| ----------- | ----------------------------------- | ------------- | -------------------------- | --------------------- |
| 1           | Обучение LogisticRegression         | обычный build | MODEL_C=1.0                | модель joblib         |
| 2           | Обучение RandomForestClassifier     | multi-stage   | N_ESTIMATORS=100           | модель joblib         |
| 3           | Обучение Ridge                      | обычный build | ALPHA=1.0                  | модель joblib         |
| 4           | Обучение GradientBoostingClassifier | multi-stage   | LEARNING_RATE=0.1          | метрики JSON          |
| 5           | Инференс сохранённой модели по CSV  | обычный build | INPUT_FILE=/data/input.csv | predictions.csv       |
| 6           | Пакетная предобработка CSV          | multi-stage   | DROP_NA=true               | clean.csv             |
| 7           | Генерация отчёта качества данных    | обычный build | REPORT_FORMAT=json         | report.json           |
| 8           | Кластеризация KMeans                | multi-stage   | N_CLUSTERS=3               | labels.csv            |
| 9           | TF-IDF-преобразование текстов       | обычный build | MAX_FEATURES=1000          | matrix.npz            |
| 10          | Извлечение признаков изображений    | multi-stage   | IMAGE_SIZE=64              | features.csv          |

## Контрольные вопросы

1\. Почему порядок инструкций в Dockerfile влияет на скорость повторной сборки?

2\. Для чего нужен файл \`.dockerignore\`?

3\. Что такое multi-stage build?

4\. Как передавать параметры приложению без пересборки образа?

5\. Чем named volume отличается от bind mount?

6\. Для чего используется HEALTHCHECK?

7\. Какие данные можно получить через \`docker inspect\`?

8\. Почему артефакты модели не всегда следует хранить внутри образа?

9\. Какие меры повышают безопасность контейнера?

10\. Как уменьшить размер Docker-образа ML-приложения?

# Лабораторная работа № 3. Создание CI/CD pipeline для ML-проекта

## Цель работы

Освоить автоматизацию проверки, сборки и публикации ML-проекта с использованием CI/CD.

## Задачи работы

- организовать репозиторий и ветвление;
- настроить автоматический запуск проверок;
- реализовать тесты данных и кода;
- собирать Docker-образ в pipeline;
- сохранять отчёты и артефакты;
- настроить условный этап доставки.

## Необходимое программное обеспечение

- ОС Windows 10/11 с WSL2 или Linux;
- Python 3.10 или новее;
- Git;
- Docker Engine / Docker Desktop;
- редактор кода или IDE.
- GitHub Actions либо Jenkins;

## Краткие теоретические сведения

CI обеспечивает частую интеграцию изменений с автоматическими проверками, а CD — подготовку или автоматическое выполнение поставки. ML-проект дополняет обычные проверки тестами данных, воспроизводимости обучения и качества модели. Pipeline должен завершаться ошибкой при нарушении критических порогов.

## Общее задание

- Создать минимум два unit-теста и один тест качества данных.
- Добавить линтер и форматтер либо статический анализатор.
- Создать workflow GitHub Actions или Jenkins Pipeline по варианту.
- Настроить кэширование Python-зависимостей.
- Добавить этап обучения на малой выборке и проверку порога метрики.
- Собрать Docker-образ после успешных тестов.
- Сохранить отчёт тестирования и метрики как артефакты CI.
- Добавить условный этап публикации образа или имитации deploy только для основной ветки или тега.
- Проверить pipeline на успешном и намеренно ошибочном commit.

## Индивидуальные варианты

| **Вариант** | **Система**                  | **Проверки**           | **Порог**                | **Этап доставки**             |
| ----------- | ---------------------------- | ---------------------- | ------------------------ | ----------------------------- |
| 1           | GitHub Actions               | pytest + ruff          | accuracy >= 0.90         | build без публикации          |
| 2           | GitHub Actions               | pytest + flake8        | F1 >= 0.85               | публикация по тегу            |
| 3           | GitHub Actions               | pytest + mypy          | RMSE <= заданного порога | build + artifact              |
| 4           | GitHub Actions               | pytest + black --check | ROC-AUC >= 0.90          | публикация main               |
| 5           | Jenkins Declarative Pipeline | pytest + pylint        | accuracy >= 0.80         | локальный registry            |
| 6           | Jenkins Declarative Pipeline | pytest + ruff          | MAE <= заданного порога  | архивирование модели          |
| 7           | GitHub Actions               | unittest + flake8      | проверка числа пропусков | build matrix Python 3.10/3.11 |
| 8           | GitHub Actions               | pytest + bandit        | silhouette >= 0.40       | security report               |
| 9           | Jenkins Pipeline             | pytest + mypy          | F1 >= 0.75               | ручное подтверждение deploy   |
| 10          | GitHub Actions               | pytest + ruff          | latency smoke-test < 1 c | публикация по release         |

## Контрольные вопросы

1\. Что является триггером CI pipeline?

2\. Чем этап CI отличается от этапа CD?

3\. Какие проверки специфичны для ML-проектов?

4\. Почему тест качества модели не заменяет unit-тесты?

5\. Для чего используется кэш зависимостей?

6\. Что такое артефакт pipeline?

7\. Как предотвратить публикацию образа при провале теста?

8\. Как безопасно хранить токены реестра контейнеров?

9\. Что такое матричная сборка?

10\. Почему pipeline должен проверяться на намеренно ошибочном изменении?

# Лабораторная работа № 4. Простая реализация ML pipeline

## Цель работы

Построить воспроизводимый модульный pipeline машинного обучения от загрузки данных до сохранения модели и метрик.

## Задачи работы

- разделить процесс на независимые этапы;
- реализовать конфигурацию эксперимента;
- исключить утечку данных;
- добавить валидацию входных данных;
- сохранять модель и метрики;
- обеспечить повторный запуск из командной строки.

## Необходимое программное обеспечение

- ОС Windows 10/11 с WSL2 или Linux;
- Python 3.10 или новее;
- Git;
- Docker Engine / Docker Desktop;
- редактор кода или IDE.
- scikit-learn, pandas, PyYAML;

## Краткие теоретические сведения

ML pipeline представляет последовательность преобразований данных и операций над моделью. Разделение этапов повышает тестируемость и повторное использование. Препроцессинг должен обучаться только на обучающей части данных. Параметры эксперимента следует отделять от программного кода.

## Общее задание

- Реализовать этапы \`load\`, \`validate\`, \`split\`, \`preprocess\`, \`train\`, \`evaluate\`, \`save\`.
- Организовать параметры в YAML/JSON-файле.
- Сформировать журнал выполнения с длительностью этапов.
- Выполнить проверку схемы данных, диапазонов и пропусков.
- Применить \`sklearn.pipeline.Pipeline\` или собственный оркестратор функций.
- Сохранить модель, конфигурацию, метрики и сведения о версии данных.
- Реализовать повторный запуск одной командой.
- Написать минимум два теста: на валидацию данных и на форму результата pipeline.

## Индивидуальные варианты

| **Вариант** | **Данные**                  | **Pipeline**                           | **Метрики**                |
| ----------- | --------------------------- | -------------------------------------- | -------------------------- |
| 1           | Iris                        | StandardScaler + LogisticRegression    | accuracy, macro-F1         |
| 2           | Wine                        | StandardScaler + SVC                   | accuracy, macro-F1         |
| 3           | Breast Cancer               | StandardScaler + RandomForest          | ROC-AUC, F1                |
| 4           | Diabetes                    | StandardScaler + Ridge                 | MAE, RMSE                  |
| 5           | Digits                      | MinMaxScaler + LogisticRegression      | accuracy, confusion matrix |
| 6           | Синтетическая классификация | SimpleImputer + RandomForest           | accuracy, ROC-AUC          |
| 7           | Синтетическая регрессия     | SimpleImputer + RandomForestRegressor  | MAE, R²                    |
| 8           | Смешанные признаки из CSV   | ColumnTransformer + LogisticRegression | F1, ROC-AUC                |
| 9           | Текстовая классификация     | TfidfVectorizer + LinearSVC            | accuracy, macro-F1         |
| 10          | Кластеризация               | StandardScaler + KMeans                | silhouette, inertia        |

## Требования к содержанию отчёта

- титульный лист с указанием дисциплины, номера и темы лабораторной работы, Ф.И.О. студента, группы и варианта;
- цель работы и индивидуальное задание;
- краткое описание архитектуры решения и используемых инструментов;
- листинги ключевых конфигурационных файлов и программного кода;
- снимки экрана или текстовые журналы, подтверждающие успешное выполнение основных этапов;
- результаты проверки, тестирования или измерения метрик;
- описание возникших ошибок и способов их устранения;
- выводы по результатам выполнения работы.

## Контрольные вопросы

1\. Какие этапы обычно включает ML pipeline?

2\. Что такое утечка данных?

3\. Почему препроцессор должен обучаться только на train-выборке?

4\. Какие преимущества даёт конфигурационный файл?

5\. Чем валидация данных отличается от оценки модели?

6\. Для чего сохранять версию данных вместе с моделью?

7\. Как обеспечить идемпотентность этапа pipeline?

8\. Почему важно логировать длительность этапов?

9\. Что должно входить в артефакты обучения?

10\. Какие части pipeline целесообразно покрыть автоматическими тестами?

# Лабораторная работа № 5. Логирование экспериментов с MLflow

## Цель работы

Освоить управление ML-экспериментами, регистрацию параметров, метрик и артефактов с использованием MLflow.

## Задачи работы

- развернуть локальный MLflow Tracking Server;
- организовать эксперимент и серии запусков;
- логировать параметры, метрики и артефакты;
- сравнить модели;
- зарегистрировать лучшую модель;
- воспроизвести выбранный запуск.

## Необходимое программное обеспечение

- ОС Windows 10/11 с WSL2 или Linux;
- Python 3.10 или новее;
- Git;
- Docker Engine / Docker Desktop;
- редактор кода или IDE.
- MLflow;

## Краткие теоретические сведения

MLflow Tracking связывает параметры, метрики, артефакты и исходные сведения о запуске. Experiment группирует связанные runs. Model Registry применяется для управления версиями модели и её стадиями или alias. Для корректного сравнения запусков необходимо фиксировать разбиение данных, seed и версию кода.

## Общее задание

- Запустить MLflow Tracking Server с локальным backend store и artifact store.
- Создать experiment с именем по шаблону группы и варианта.
- Выполнить серию не менее чем из 8 запусков по сетке параметров.
- Логировать параметры модели, seed, метрики train и test, длительность обучения.
- Сохранить график или матрицу ошибок, конфигурацию и модель как артефакты.
- Выбрать лучший запуск по основной метрике и обосновать выбор.
- Зарегистрировать модель в Model Registry и назначить alias \`candidate\`.
- Загрузить модель из MLflow и выполнить контрольный инференс.
- Экспортировать таблицу сравнений запусков в отчёт.

## Индивидуальные варианты

| **Вариант** | **Модель**                 | **Исследуемые параметры**   | **Критерий выбора**         |
| ----------- | -------------------------- | --------------------------- | --------------------------- |
| 1           | LogisticRegression         | C: 0.01–10; penalty         | macro-F1                    |
| 2           | RandomForestClassifier     | n_estimators, max_depth     | ROC-AUC                     |
| 3           | SVC                        | C, gamma, kernel            | accuracy                    |
| 4           | GradientBoostingClassifier | n_estimators, learning_rate | F1                          |
| 5           | Ridge                      | alpha                       | RMSE (минимум)              |
| 6           | RandomForestRegressor      | n_estimators, max_depth     | MAE (минимум)               |
| 7           | KNeighborsClassifier       | n_neighbors, weights        | accuracy                    |
| 8           | KMeans                     | n_clusters, init            | silhouette                  |
| 9           | Tfidf + LinearSVC          | ngram_range, C              | macro-F1                    |
| 10          | Pipeline с выбором модели  | тип модели + её параметры   | составная: качество и время |

## Требования к содержанию отчёта

- титульный лист с указанием дисциплины, номера и темы лабораторной работы, Ф.И.О. студента, группы и варианта;
- цель работы и индивидуальное задание;
- краткое описание архитектуры решения и используемых инструментов;
- листинги ключевых конфигурационных файлов и программного кода;
- снимки экрана или текстовые журналы, подтверждающие успешное выполнение основных этапов;
- результаты проверки, тестирования или измерения метрик;
- описание возникших ошибок и способов их устранения;
- выводы по результатам выполнения работы.

## Контрольные вопросы

1\. Что представляет собой run в MLflow?

2\. Чем параметр отличается от метрики?

3\. Что хранится в artifact store?

4\. Для чего нужен backend store?

5\. Почему seed следует логировать?

6\. Как выбрать основную метрику эксперимента?

7\. Какие сведения нужны для воспроизведения запуска?

8\. Что такое Model Registry?

9\. Для чего используются aliases или стадии модели?

10\. Почему максимальная test-метрика не всегда означает лучшую производственную модель?

# Лабораторная работа № 6. Отслеживание изменений в моделях и данных с DVC

## Цель работы

Научиться версионировать данные, модели и этапы ML-процесса с использованием DVC совместно с Git.

## Задачи работы

- инициализировать DVC;
- настроить удалённое хранилище;
- добавить данные и модель под контроль DVC;
- описать pipeline в dvc.yaml;
- сравнить версии данных и метрик;
- воспроизвести ранее зафиксированное состояние проекта.

## Необходимое программное обеспечение

- ОС Windows 10/11 с WSL2 или Linux;
- Python 3.10 или новее;
- Git;
- Docker Engine / Docker Desktop;
- редактор кода или IDE.
- DVC;

## Краткие теоретические сведения

Git эффективно версионирует текстовые файлы, но не предназначен для больших наборов данных и бинарных моделей. DVC хранит в Git небольшие метафайлы и связывает их с объектами в кэше или удалённом хранилище. \`dvc.yaml\` описывает зависимости, команды и выходы этапов, а \`dvc.lock\` фиксирует конкретное состояние pipeline.

## Общее задание

- Инициализировать Git и DVC в проекте.
- Настроить локальный remote-каталог, расположенный вне репозитория.
- Добавить исходные данные под DVC-контроль и выполнить push.
- Описать минимум три этапа: prepare, train, evaluate.
- Объявить параметры, зависимости, выходы и файл метрик.
- Выполнить первую версию pipeline и commit метафайлов.
- Изменить данные согласно варианту, пересчитать pipeline и сравнить метрики через DVC.
- Создать Git-тег для каждой версии.
- Переключиться на предыдущий тег, выполнить \`dvc pull\` и проверить воспроизводимость модели.
- Сформировать граф pipeline и включить его в отчёт.

## Индивидуальные варианты

| **Вариант** | **Изменение данных**                   | **Метрики**    | **Цель анализа**                |
| ----------- | -------------------------------------- | -------------- | ------------------------------- |
| 1           | Добавить 10% корректных объектов       | accuracy/F1    | ожидается устойчивость или рост |
| 2           | Удалить 10% объектов одного класса     | macro-F1       | проанализировать дисбаланс      |
| 3           | Добавить 5% пропусков                  | MAE/accuracy   | изменить preprocessing          |
| 4           | Добавить шум к числовым признакам      | ROC-AUC        | оценить деградацию              |
| 5           | Изменить правило train/test split      | accuracy       | сравнить вариативность          |
| 6           | Добавить новый признак                 | MAE/R²         | оценить полезность              |
| 7           | Удалить один признак                   | F1             | оценить влияние                 |
| 8           | Изменить словарь текстов               | macro-F1       | сравнить размер словаря         |
| 9           | Изменить разрешение изображений        | accuracy/время | сравнить ресурсы                |
| 10          | Изменить число кластеров/состав данных | silhouette     | сравнить структуру              |

## Контрольные вопросы

1\. Почему большие данные не рекомендуется хранить непосредственно в Git?

2\. Что содержит \`.dvc\`-файл?

3\. Для чего нужен DVC cache?

4\. Чем \`dvc.yaml\` отличается от \`dvc.lock\`?

5\. Что означают deps и outs в этапе DVC?

6\. Как DVC определяет необходимость повторного запуска этапа?

7\. Для чего используется \`dvc push\` и \`dvc pull\`?

8\. Как связаны Git-тег и версия данных DVC?

9\. Как сравнить метрики нескольких ревизий?

10\. Какие риски возникают при недоступности удалённого DVC-хранилища?

# Лабораторная работа № 7. Сервис на FastAPI для модели

## Цель работы

Развернуть обученную модель машинного обучения в виде документированного REST API-сервиса.

## Задачи работы

- спроектировать API-контракт;
- реализовать валидацию запросов;
- загрузить модель при старте приложения;
- создать endpoints инференса и проверки состояния;
- обработать ошибки;
- контейнеризировать и протестировать сервис.

## Необходимое программное обеспечение

- ОС Windows 10/11 с WSL2 или Linux;
- Python 3.10 или новее;
- Git;
- Docker Engine / Docker Desktop;
- редактор кода или IDE.
- FastAPI, Uvicorn, pytest, httpx;

## Краткие теоретические сведения

Сервис инференса отделяет модель от потребителя и предоставляет стабильный программный интерфейс. FastAPI использует модели Pydantic для проверки типов и автоматически формирует OpenAPI-документацию. Производственный сервис должен иметь health/readiness endpoints, структурированное логирование, предсказуемые коды ответа и ограничения входных данных.

## Общее задание

- Подготовить сохранённую модель и схему входных признаков.
- Реализовать endpoint \`GET /health\` и \`GET /model-info\`.
- Реализовать \`POST /predict\` для одного объекта и \`POST /predict-batch\` для списка объектов.
- Добавить Pydantic-модели входа и выхода, диапазоны допустимых значений и примеры.
- Загружать модель один раз при старте приложения.
- Добавить обработчики ошибок и структурированное логирование без записи чувствительных данных.
- Написать минимум 6 тестов с TestClient, включая невалидные запросы.
- Собрать Docker-образ и запустить сервис с пробросом порта.
- Проверить Swagger UI и измерить задержку не менее 100 последовательных запросов.

## Индивидуальные варианты

| **Вариант** | **Модель / сервис**            | **Вход**                         | **Выход**                 |
| ----------- | ------------------------------ | -------------------------------- | ------------------------- |
| 1           | Iris classifier                | 4 числовых признака              | класс + вероятности       |
| 2           | Wine classifier                | 13 признаков                     | класс + confidence        |
| 3           | Breast Cancer classifier       | выбранные признаки               | класс + вероятность риска |
| 4           | Diabetes regressor             | 10 признаков                     | числовой прогноз          |
| 5           | Digits classifier              | массив 64 значений               | цифра + top-3             |
| 6           | Синтетический churn classifier | числовые и категориальные поля   | класс + вероятность       |
| 7           | Синтетический price regressor  | числовые и категориальные поля   | цена + версия модели      |
| 8           | KMeans segmenter               | набор признаков                  | кластер + расстояние      |
| 9           | Text classifier                | строка текста                    | метка + score             |
| 10          | Image feature classifier       | base64 или путь в тестовой среде | класс + score             |

## Требования к содержанию отчёта

- титульный лист с указанием дисциплины, номера и темы лабораторной работы, Ф.И.О. студента, группы и варианта;
- цель работы и индивидуальное задание;
- краткое описание архитектуры решения и используемых инструментов;
- листинги ключевых конфигурационных файлов и программного кода;
- снимки экрана или текстовые журналы, подтверждающие успешное выполнение основных этапов;
- результаты проверки, тестирования или измерения метрик;
- описание возникших ошибок и способов их устранения;
- выводы по результатам выполнения работы.

## Контрольные вопросы

1\. Что такое REST API?

2\. Для чего FastAPI использует Pydantic?

3\. Почему модель следует загружать один раз при старте?

4\. Чем health check отличается от readiness check?

5\. Какие HTTP-коды подходят для ошибок валидации и внутренних ошибок?

6\. Почему нельзя логировать все входные данные без ограничений?

7\. Как проверить API без запуска внешнего клиента?

8\. Что содержит OpenAPI-схема?

9\. Какие особенности имеет batch-инференс?

10\. Какие меры нужны для масштабирования сервиса инференса?

# Лабораторная работа № 8. Мониторинг модели: Prometheus и Grafana

## Цель работы

Организовать сбор, хранение и визуализацию технических и модельных метрик ML-сервиса.

## Задачи работы

- добавить метрики в FastAPI-сервис;
- развернуть Prometheus и Grafana;
- создать dashboard;
- сгенерировать нагрузку и ошибки;
- настроить правила предупреждений;
- проанализировать признаки деградации сервиса или модели.

## Необходимое программное обеспечение

- ОС Windows 10/11 с WSL2 или Linux;
- Python 3.10 или новее;
- Git;
- Docker Engine / Docker Desktop;
- редактор кода или IDE.
- Prometheus, Grafana, Docker Compose, prometheus-client.

## Краткие теоретические сведения

Мониторинг ML-систем включает технические метрики сервиса, показатели качества данных и модели, а также бизнес-метрики. Prometheus периодически собирает метрики в формате временных рядов, а Grafana визуализирует их на dashboard. Для метрик используются типы Counter, Gauge, Histogram и Summary. Истинное качество модели часто доступно с задержкой, поэтому дополнительно наблюдают распределения входов и прогнозов.

## Общее задание

- Расширить сервис лабораторной работы №7 endpoint \`/metrics\`.
- Добавить счётчик запросов по endpoint и статусу, histogram задержки, gauge активных запросов и счётчик ошибок.
- Добавить не менее двух ML-метрик по варианту.
- Создать Docker Compose для сервиса, Prometheus и Grafana.
- Настроить scrape-конфигурацию Prometheus.
- Создать Grafana dashboard минимум из 6 панелей.
- Сгенерировать нормальную нагрузку, всплеск нагрузки и серию невалидных запросов.
- Настроить минимум два alert rule: технический и модельный.
- Смоделировать условие срабатывания и сохранить подтверждение.
- Сформулировать вывод о состоянии сервиса и возможной деградации модели.

## Индивидуальные варианты

| **Вариант** | **ML-метрика**                     | **Модельный alert**           | **Технический alert**       |
| ----------- | ---------------------------------- | ----------------------------- | --------------------------- |
| 1           | Доля классов прогнозов             | рост доли одного класса > 80% | p95 latency > 0.5 c         |
| 2           | Средняя confidence                 | confidence < 0.6              | error rate > 5%             |
| 3           | Число значений вне train-диапазона | out-of-range > 10%            | service down                |
| 4           | Сдвиг среднего ключевого признака  | отклонение > 2σ               | p99 latency > 1 c           |
| 5           | Доля пропусков/нулевых значений    | missing > 5%                  | requests/sec > лимита       |
| 6           | Распределение batch size           | batch > 100                   | memory proxy gauge > порога |
| 7           | Средний прогноз регрессии          | изменение > 30% baseline      | error rate > 3%             |
| 8           | Распределение кластеров            | пустой/доминирующий кластер   | p95 > 0.7 c                 |
| 9           | Длина текста и доля пустых строк   | empty > 2%                    | HTTP 422 > порога           |
| 10          | Размер изображения/вектора         | invalid shape > 1%            | throughput < порога         |

## Контрольные вопросы

1\. Какие группы метрик применяются для мониторинга ML-систем?

2\. Чем Counter отличается от Gauge?

3\. Когда следует использовать Histogram?

4\. Как Prometheus получает метрики приложения?

5\. Что такое label и почему высокая кардинальность опасна?

6\. Как вычислить долю ошибок по временным рядам?

7\. Что показывает p95 задержки?

8\. Чем data drift отличается от concept drift?

9\. Почему отсутствие ground truth усложняет мониторинг качества модели?

10\. Какие элементы должен содержать полезный Grafana dashboard?

# Рекомендуемая структура репозитория лабораторного практикума

mlops-labs/  
├── lab01/  
├── lab02/  
├── lab03/  
├── lab04/  
├── lab05/  
├── lab06/  
├── lab07/  
├── lab08/  
├── .gitignore  
└── README.md

Допускается ведение отдельных репозиториев. В этом случае каждый репозиторий должен содержать собственный README, файл зависимостей, исходный код, тесты и конфигурационные файлы, необходимые для воспроизведения результата.

# Рекомендуемые источники

- Официальная документация Docker.
- Официальная документация GitHub Actions или Jenkins.
- Официальная документация scikit-learn.
- Официальная документация MLflow.
- Официальная документация DVC.
- Официальная документация FastAPI.
- Официальная документация Prometheus и Grafana.
